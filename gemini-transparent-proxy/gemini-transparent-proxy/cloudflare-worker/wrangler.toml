name = "gemini-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# 可选: 如果需要使用 KV 存储或其他 Cloudflare 资源，取消下面的注释
# [kv_namespaces]
# PROXY_KV = "gemini-proxy-cache"

# 可选: 设置环境变量 (通过 wrangler secret put 配置更安全)
# [vars]
# GOOGLE_API_KEY = "your-api-key"